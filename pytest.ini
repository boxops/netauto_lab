[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: marks tests as integration (requires running services, use --run-integration to include)
    unit: marks tests as unit tests (no external services needed)
